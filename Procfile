web: python server.py
# worker and stats are no longer separate processes; bot runs inside server.py thread
# If you need to run them separately locally, uncomment below
# worker: python main.py
# stats: python stats.py